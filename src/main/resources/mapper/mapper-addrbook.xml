<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-addrbook">
	
	<select id="getDB" parameterType="int" resultType="lx.edu.springboot.vo.AddrBookVO">
		select AB_ID abId
			,AB_NAME abName
			,ab_email 
			,ab_comdept 
			,ab_birth 
			,ab_tel 
			,ab_memo 
		from addrbook
		where ab_id = #{abId};
	
	</select>
	
	<select id="getDBList" resultType="lx.edu.springboot.vo.AddrBookVO">
		select AB_ID abId
			,AB_NAME abName
			,ab_email 
			,ab_comdept 
			,ab_birth 
			,ab_tel 
			,ab_memo 
		from addrbook		
	</select>
	
	<insert id="insertDB" parameterType="lx.edu.springboot.vo.AddrBookVO">
		insert into addrbook(ab_name, ab_email, ab_comdept, ab_birth, ab_tel, ab_memo)
			values(#{abName}, #{abEmail}, #{abComdept}, #{abBirth}, #{abTel}, #{abMemo});
	</insert>
	
	<update id="updateDB" parameterType="lx.edu.springboot.vo.AddrBookVO">
	    UPDATE addrbook
	    SET ab_name = #{abName}, ab_email = #{abEmail}, ab_comdept = #{abComdept},
	        ab_birth = #{abBirth}, ab_tel = #{abTel}, ab_memo = #{abMemo}
	    WHERE ab_id = #{abId}
	</update>
	
	<delete id="deleteDB" parameterType="int">
		delete from addrbook where ab_id = #{abId};
		
	</delete>

</mapper>